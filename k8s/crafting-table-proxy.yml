apiVersion: apps/v1
kind: Deployment
metadata:
  name: crafting-table-proxy
  namespace: crafting-table-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crafting-table-proxy
  template:
    metadata:
      labels:
        app: crafting-table-proxy
    spec:
      containers:
        - name: proxy
          image: crafting-table-proxy:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          env:
            - name: REDIS_URL
              # Refers to the service in the same namespace
              value: "redis://redis-service:6379"
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: crafting-table-proxy-svc
  namespace: crafting-table-apps
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: crafting-table-proxy